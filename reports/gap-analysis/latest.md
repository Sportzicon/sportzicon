# Sportivox · Daily Gap Analysis — 2026-05-31

Generated by `scripts/gap-analysis/gap-analysis.mjs`.

| Severity | Count |
|---|---|
| P0 (critical) | 0 |
| P1 (high) | 2 |
| P2 (medium) | 4 |
| P3 (low) | 4 |
| **Total** | **10** |

## Findings

### [P1] production · Scoring backend lacks structured logging

**Evidence:** `scoring/backend/src/app.ts`

**Suggested action:** Add pino-http (matches main backend) so Cloud Logging can parse fields.

---

### [P1] production · Scoring backend has no rate limiting

**Evidence:** `scoring/backend/src/app.ts`

**Suggested action:** Add express-rate-limit (matches main backend's apiLimiter).

---

### [P2] production · No graceful shutdown handler

**Evidence:** `scoring/backend/src/server.ts`

**Suggested action:** Listen for SIGTERM and close the HTTP server + DB pool to drain in-flight requests cleanly.

---

### [P2] production · CI does not exercise the scoring subproject

**Evidence:** `.github/workflows/ci.yml`

**Suggested action:** Add a job that runs `cd scoring && npm ci && npm run build` so PRs catch scoring regressions.

---

### [P2] architecture · Two prisma schemas in the same repo

**Evidence:** `backend/prisma/schema.prisma + scoring/backend/prisma/schema.prisma`

**Suggested action:** Document the boundary (which schema owns which models) or merge — duplicate User models risk drift.

---

### [P2] architecture · ARCHITECTURE.md has no diagrams

**Evidence:** `docs/ARCHITECTURE.md`

**Suggested action:** Add at least one Mermaid C4 / context diagram so new engineers can orient.

---

### [P3] security · npm audit failed for backend

**Evidence:** `Command failed: npm audit --json --omit=dev`

**Suggested action:** Run npm install first, then re-run audit.

---

### [P3] security · npm audit failed for scoring/backend

**Evidence:** `Command failed: npm audit --json --omit=dev`

**Suggested action:** Run npm install first, then re-run audit.

---

### [P3] security · npm audit failed for scoring/frontend

**Evidence:** `Command failed: npm audit --json --omit=dev`

**Suggested action:** Run npm install first, then re-run audit.

---

### [P3] architecture · No Storybook / component catalogue

**Evidence:** `(repo-wide)`

**Suggested action:** Consider Storybook so the design system is discoverable. Optional for v1.

---

